"use client";

import ScrollInView from "../reusable/ScrollInView";

function Mission() {
  return (
    <ScrollInView className="m-4 md:m-20 bg-primary rounded-2xl text-white p-4 md:p-8">
      <h3 className="text-4xl md:text-6xl font-semibold">
        I want to build software that not only works but wows – simple, smart, and built to last.
      </h3>
    </ScrollInView>
  );
}

export default Mission;